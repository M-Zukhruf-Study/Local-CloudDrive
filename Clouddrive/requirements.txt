# Core runtime dependencies for CloudDrive
# Install on Linux/macOS: python3 -m pip install -r requirements.txt
# Install on Windows:     py -m pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
aiofiles==23.2.1
python-dotenv==1.0.1

# If you get a dependency error during setup, run the install command again
# after activating your virtual environment.
